let API_BASE = "";

if (typeof window !== "undefined") {
  const hostname = window.location.hostname;

  if (hostname === "localhost") {
    API_BASE = "http://localhost:5000";
  } else if (hostname === "cf-server-tr24.onrender.com") {
    API_BASE = "https://cf-server-tr24.onrender.com";
  } else {
    API_BASE = "https://server.cinemafactory.co.in";
  }
}

export default API_BASE;
